{
  "buildCommand": "cd Writer-Assistant && pnpm install && pnpm --filter @workspace/writer run build",
  "outputDirectory": "Writer-Assistant/artifacts/writer/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
